{
  "name": "architecture-control-plane",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run lint && npm run test",
    "lint": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "smoke": "node scripts/live-smoke.mjs",
    "smoke:local": "node scripts/live-smoke.mjs http://127.0.0.1:8787",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.8",
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.83.0"
  }
}
