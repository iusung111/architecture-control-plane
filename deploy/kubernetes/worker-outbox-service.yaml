apiVersion: v1
kind: Service
metadata:
  name: acp-worker-outbox-metrics
  labels:
    app.kubernetes.io/component: worker-outbox
spec:
  selector:
    app.kubernetes.io/component: worker-outbox
  ports:
    - name: metrics
      port: 9102
      targetPort: metrics
